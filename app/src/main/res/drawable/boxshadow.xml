<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/white" /> <!-- Màu nền -->
    <corners android:radius="@dimen/text_size_sm" /> <!-- Góc bo tròn -->
    <padding
        android:left="2dp"
        android:top="2dp"
        android:right="2dp"
        android:bottom="2dp" /> <!-- Khoảng cách bóng đổ -->
    <gradient
        android:startColor="#33000000"
        android:endColor="#33000000"
        android:angle="270" /> <!-- Màu bóng đổ -->
</shape>